# 1. 必须忽略的文件夹（这些是自动生成的，不需要上传，否则会导致冲突）
/[Ll]ibrary/
/[Tt]emp/
/[Oo]bj/
/[Bb]uild/
/[Bb]uilds/

# 2. 日志文件（Unity 运行时生成，必须忽略）
/[Ll]ogs/

# 3. 必须保留的文件（确保没有写 *.meta 这一行！）
# 千万不要写 *.meta，否则你的图片引用会全部丢失！

# 3. 系统文件
.DS_Store